# Legacy thesis evidence

This directory records evidence distilled from the thesis-era repository and historical branches.

Historical sources include `main`, `training_rf`, `training_svm`, and `cleaning`. They are evidence/baselines, not V2 semantic authority. Do not merge or rewrite them as part of specification work.
